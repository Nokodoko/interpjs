class environment(){
    constructor(record = {}){
        this.record = record;
    }
    //
    //define (set)
    define(name, value){
        this.record[name] = value;
    }
    //lookup 
    //assign (self-evaluate)
}
